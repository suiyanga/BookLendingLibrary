one:
  email: "user1@example.com"
  encrypted_password: <%= User.digest('password') %>
  # other required fields for user

two:
  email: "user2@example.com"
  encrypted_password: <%= User.digest('password') %>
  # other required fields for user
